# 管理画面

`https://iridescent-pothos-77fe76.netlify.app/admin.html`（手元では `npm run serve` のあと `http://localhost:4199/admin.html`）

## 何が見えるか

| 欄 | 中身 |
| --- | --- |
| 成績(ranks) | オンラインの持ち点つき対局を終えた端末が置いた成績。名前・称号・持ち点・位・対局数・勝数・アイコン・最終更新・端末id |
| 待ち合わせ(lobby) | 相手を待っている部屋。対局が始まるか3分たつと消える |

名前か端末idで探せる。持ち点／最終更新／対局数／名前で並べ替えられる。

## できること・できないこと

- 行を **消す** ことはできる（名前が不適切なときなど）。ただし本人の端末の記録は消えないので、次の持ち点つき対局でまた載る
- 名前や持ち点を **書き換えることはできない**。本人の端末が正
- **CPU戦だけの人は出ない**。サーバーに何も送っていないため
- 同じ人が端末を替えると **別の行** になる（本人確認が無い）

## いまの限界

- アカウントは端末の中だけ。サーバーに本人の確認は無い。端末を替えても続くアカウントにするには Sign in with Apple や Game Center が要る
- 読み書きは本体と同じ REST で、Firebase のルール上は誰でもできる。この画面が特別な権限を持っているわけではない。管理者だけが消せるようにするには、ルールに認証を入れる必要がある
- 管理画面の URL は隠していないが、`noindex` にして検索には載らないようにしてある

## 通報を読む

2026-09-04 に通報(ガイドライン 1.2)を足した。利用者はランキングの各行の
右端「⋯」から、相手を通報したり、見えなくしたりできる。

通報は `reports/<自動id>` に貯まる。中身は
`{targetId, targetName, reason, reporterId, reporterName, at, handled}`。

**この画面からは読めない。** `firebase-rules.json` で `reports` を
`".read": false` にしてあるため。通報には「誰が誰を通報したか」が入るので、
ランキングと同じく誰でも読める場所に置くわけにいかない。

読むときは **Firebase のコンソール**を開く。

```
https://console.firebase.google.com/
→ プロジェクト tottery-66e0f → Realtime Database → reports
```

対応したら、その通報の `handled` を true にして、必要なら
この管理画面から該当の `ranks` の行を消す。

**24時間以内に対応すること。** アプリの中で利用者にそう約束している
(`src/ui/report.jsx`)。プライバシーポリシーにも書いてある。

この画面から通報を読めるようにするには、管理画面に認証(Firebase Auth)を
入れる必要がある。いまは端末の目印しか無く、管理者を見分けられない。
