# 管理画面

配信先の URL の `/admin.html`（Cloudflare。Netlify は使わない）。手元では `npm run serve` のあと `http://localhost:4199/admin.html`

## 何が見えるか

| 欄 | 中身 |
| --- | --- |
| 登録した人(players) | **名前を決めた端末すべて**。名前・称号・持ち点・対局数・勝数・アイコン・登録日・最終更新・状態・端末id。この版より前に名前を決めた人は、次にアプリを開いたときに載る |
| 持ち点つきの成績(ranks) | オンラインの持ち点つき対局を終えた端末が置いた成績 |
| 運営からのお知らせ(letters) | 宛先（全員／特定の人）・件名・本文・添付を選んで出す。出した手紙の一覧から取り消せる（まだ受け取っていない人にだけ効く） |
| 待ち合わせ(lobby) | 相手を待っている部屋。対局が始まるか3分たつと消える |

名前か端末idで探せる。持ち点／最終更新／対局数／名前で並べ替えられる。

**名前の一覧から詳細へ。** 一覧には名前と、そえ書き（称号・最終更新／持ち点）だけを出す。行を押すと詳細が開き、そこに全部の項目と「使用停止」「消す」が並ぶ。スマホでは下から、広い画面では真ん中に出る。背景か × で閉じる。

## できること・できないこと

- 行を **消す** ことはできる。ただしサーバーの記録が消えるだけで、端末は次に開いたときにまた載る
- **使用停止** にすると、その端末は次に開いたときに名前を失い、決め直しの画面になる（対局数などの記録も消える）。同じ端末で別の名前を決め直すことはできる（本人確認が無いため）。「解除」で戻せる
- 名前や持ち点を **書き換えることはできない**。本人の端末が正
- **CPU戦だけの人は出ない**。サーバーに何も送っていないため
- 同じ人が端末を替えると **別の行** になる（本人確認が無い）

## Firebase のルール

`players` の読み書きを許すルールが要る。[firebase-rules.json](firebase-rules.json) をそのまま Firebase のコンソール（Realtime Database → ルール）に貼って公開する。公開するまで、管理画面の「登録した人」は「players は読めません」と出る。

## いまの限界

- アカウントは端末の中だけ。サーバーに本人の確認は無い。端末を替えても続くアカウントにするには Sign in with Apple や Game Center が要る
- 読み書きは本体と同じ REST で、Firebase のルール上は誰でもできる。この画面が特別な権限を持っているわけではない。管理者だけが消せるようにするには、ルールに認証を入れる必要がある
- 管理画面の URL は隠していないが、`noindex` にして検索には載らないようにしてある
