# 広告リワード（広告を見てガチャ1回・1日3回）

2026-09-12 本人の決め: 広告を1本見ると**ガチャ1回ぶん（チケット1枚）**もらえる。**1日3回まで**。

## 作り（できているもの）

| 何 | どこ | 要点 |
| --- | --- | --- |
| 回数と報酬 | `src/iap/catalog.js` | `ADS_PER_DAY=3`、`AD_REWARD_TICKETS=1` |
| サーバー | `src/server/wallet.js` `adReward()` | 1日の回数はサーバーが数える（kind='ad'、ref=日付）。出来事 id で冪等。上限超過は拒む。要約に `adsLeftToday`（now を渡したときだけ） |
| 口 | `src/server/worker.js` | `/api/wallet/ad-reward`（本人確認つき）。`/api/wallet/summary` は残り回数も返す |
| アプリ | `src/net/ads.js` | AdMob のリワード動画を出し、最後まで見たらサーバーに報酬を頼む。Web では出さない |
| 画面 | `src/ui/skins.jsx` | ガチャ画面に「広告を見てチケット1枚（今日あとN回）」。残り0で消える |
| 検査 | `tools/check-wallet.mjs`（+7件） | 3回でチケット+1ずつ・上限超過拒否・冪等・翌日回復・now無しは残り回数を入れない |

**回数はサーバーが正**。端末をいじっても1日3回を超えては配られない（ただし広告を実際に見たかの検証は下記の残り課題）。

## ID は2種類（置き場所が違う）

| ID | 実値 | どこへ |
| --- | --- | --- |
| **アプリ ID**（`〜`） | `ca-app-pub-8562097921065694~4474462397` | iOS の `Info.plist`（`GADApplicationIdentifier`）★iOS セッション |
| **広告ユニット ID**（`/`） | `ca-app-pub-8562097921065694/4541553185` | ビルドに埋め込み済み（`build.mjs` の既定）★対応不要 |

**受領済み(2026-09-12)。広告ユニット ID はコードに埋め込み済み。残るはアプリ ID を Info.plist に入れることだけ。**

## ご本人がやること — AdMob（apps.admob.com）

**A-1. AdMob に登録**
1. apps.admob.com に Google アカウントでサインイン。初回は AdMob の利用規約に同意（無料）。
2. お金を受け取るには後で「お支払い」に住所・税務・銀行を登録するが、**広告ユニットを作って試すだけなら不要**。

**A-2. アプリを登録して「アプリ ID」を得る**
1. 左メニュー **アプリ** → **アプリを追加**。
2. プラットフォーム **iOS** →「アプリはストアに公開されていますか？」→ まだなら **いいえ**（あとで App Store と紐づけられる）。
3. アプリ名 **Tottery**（または トッタリー）→ 追加。
4. 作成後の画面に **アプリ ID**（`ca-app-pub-…〜…`、間が **〜**）が出る。控える。

**A-3. リワード動画の「広告ユニット ID」を得る**
1. そのアプリの **広告ユニット** → **広告ユニットを追加** → **リワード**。
2. 名前（例：Gacha Rewarded）。報酬の欄は「数量 1／アイテム名 ticket」など任意でよい（**実際の報酬はサーバーが決める**ので飾り）。
3. 作成 → **広告ユニット ID**（`ca-app-pub-…/…`、間が **/**）が出る。控える。

**A-4. 私に2つの ID を渡す**（アプリ ID と 広告ユニット ID）。

**A-5. App Store Connect の申告**（`reports/appstore/App Store提出.md` の「課金・広告を入れるとき」）
- 「アプリのプライバシー」で、**第三者（Google AdMob）が「識別子（デバイス ID）」と「使用状況データ」を広告のために収集**する、と申告（用途「サードパーティの広告」）。
- AdMob は広告 ID を使うので、**ATT（トラッキング許可のダイアログ）**が要る。`NSUserTrackingUsageDescription` の文言を決める（例：「あなたに合った広告を表示するために使用します」）。
- 審査メモ・説明文の「広告はありません」を、広告ありに書き換える。
- 年齢制限は本ゲームの内容では 4+ のまま。AdMob 側で「子ども向け扱い（TFCD/TFUA）」にするかを決める（子ども向けにすると広告が制限される）。
- お金を受け取るには AdMob の「お支払い」設定（住所・税務・しきい値到達後に銀行）を済ませる。

## iOS セッションがやること

1. `ios/App/App/Info.plist` に3つ足す：
   - `GADApplicationIdentifier` = **`ca-app-pub-8562097921065694~4474462397`**(A-2 のアプリ ID)。
   - `SKAdNetworkItems` = Google が公開している SKAdNetwork ID の一覧（AdMob のドキュメントの plist をそのまま貼る）。
   - `NSUserTrackingUsageDescription` = A-5 で決めた文言。
2. `npx cap sync ios`（プラグイン `@capacitor-community/admob` は package.json に入れてある）。
3. ATT の許可を求める処理は **`ads.js` の初期化に入れた**（広告を出す前に一度だけダイアログ）。iOS 側は Info.plist の文言追加だけでよい。

## 私（Claude）がやること

- 広告ユニット ID は `build.mjs` の既定に埋め込み済み(`ca-app-pub-8562097921065694/4541553185`)。
- **テスト広告と本物の切り替え**: 既定は**テスト広告**(`isTesting:true`)。TestFlight・手元は
  これで安全に試せる(本物のユニット ID のままテスト広告が出るので規約違反にならない)。
  **App Store の配信ビルドだけ** `ADMOB_TESTING=false npm run ios:testflight` にすると本物の広告になる。
  → 自分たちのテストで本物の広告を再生して無効なトラフィックを出さないための仕組み。
- ATT の許可要求は `ads.js` の初期化に入れた(広告の直前に一度だけ。断られてもパーソナライズなし広告として動く)。

## 試す順番

1. push で Worker（`/api/wallet/ad-reward`）は本番に出る。古いアプリには影響しない。
2. TestFlight を配ると、テスト広告で試せる。**ボタンが出るのは `FREE_GACHA=false` のとき**（無料ガチャ中は出ない。テストするときは一時的に false に）。
3. 広告を最後まで見る→チケット+1、残り回数が減る。3回で消える。翌日（UTC）復活。

## まだ塞げていないこと（承知のうえ）

- **広告を実際に見たかをサーバーで検証していない**（AdMob の SSV＝サーバーサイド検証は未実装）。いまは「アプリが報酬を頼む」形で、1日3回の上限だけが歯止め。悪用対策を強めるなら AdMob の SSV コールバックを Worker で受ける。
- 報酬はチケット（端末で使う）。ガチャの抽選・所持は端末のまま。
