# 盤面エリア（試験ルール）

2026-09-08 に決めた仕様。ルール層は入っている（`src/game/areas.js`）。
**画面にはまだ出していない。** 9×9 だけ。5×5 には無い。

## 決まり

- **立つ条件**: 王に選んだ札のランクにスキンを装備していること。
  対局開始時（布陣が揃ったとき）に決まり、その局のあいだ変わらない。
  王が倒れて継承しても、エリアは最初のまま
- **発動**: 1局に1回。自分の手番の初め（まだ何もしていないとき）に「発動する／しない」を選ぶ。
  宮殿以外は手番を消費しない。**発動は相手にも見える**（何が起きたかは分かる）。
  見抜いた正体だけは、見抜いた側にしか見えない
- **回数は後で調整する**（本人の意向）。いまは1局1回で固定

| 王のランク | エリア | 効果 | 手番 |
| --- | --- | --- | --- |
| 2・3 | 土 | 直前に動いた相手の駒の足跡を読み、**50%** で正体を見抜く（自分だけが知る）。相手が動いていないとき・その駒の正体をすでに知っているときは使えない | 使わない |
| 4・5 | 海 | 盤上の全駒を中央へ引き寄せる。取りは起きない | 使わない |
| 6・7 | 森 | 相手の王以外から **3体**を見抜く（自分だけが知る）。全公開だと消去法で王が割れるので3体 | 使わない |
| 8・9 | 氷 | 相手の王以外を凍らせ、**相手の3手番**動けなくする。凍った駒は取られる。凍った駒は A の入れ替えにも使えない | 使わない |
| 10 | 空 | 自分の駒1体（王・10・A 以外）をこの局のあいだ **10 の動き**にする。ランク自体は変わらない（正体・合計判定・採用枚数に影響しない）。その駒は1手番に2回動ける | 使わない |
| J・Q・K | 宮殿 | 自分の駒1体（王以外の 2〜9）を **1段昇格**（上限 10）。採用枚数の表は追いかけるが、合計判定に使う「最初の採用合計」は変えない | **使う** |

### 海の引き寄せ方

中央に近い駒から順に（同じ近さなら手番側の駒、次に行・列の順）、
「中央にいちばん近い空きマス」へ置く。同じ近さなら元の位置に近いマス。
自分のいたマスも空きなので、遠ざかることはない。9×9 の 18体は中央の 5×5 に収まる。

### 氷で手番が飛ぶとき

凍った駒のせいで、その側に指せる手が1つも無ければ手番を飛ばす（負けにはしない）。
飛ばすのは1回だけで、相手にも手が無ければ従来の引き分け判定に任せる。
王が A なら入れ替えが残るので、まず飛ばない。

## 作り

- **状態**: `areasEnabled`（START_SETUP の `areas: true` と 9×9）、`areaLoadouts`
  （START_SETUP の `loadouts: [{rank: skinId}, {rank: skinId}]`）、`areas: [{type, used, rank, skin} | null, …]`、
  `known: [{pieceId: true}, {…}]`（見抜いた駒）、`turnNo`（氷の解ける時期を数える）、`lastArea`（演出用）
- **駒**: `frozenUntil`（氷）、`moveAs: "10"` と `skyTwice`（空）
- **手**: `USE_AREA { pieceId?, hit?, picks? }`。土の `hit` と森の `picks` は `enrichAction` が焼き込む。
  通信で届いた手にそれが無ければ捨てる（`seedsPresent`）。`NET_ACTIONS` に入っている
- **合法手**: `getLegalMoves` は `piece.moveAs || piece.rank` で動きを決める。
  `hasLegalAction`（引き分け判定）は凍った駒を数えない
- **CPU**: 使えるなら使う。空は一番強い駒を変身、宮殿は取れる手が無い序盤（turnNo ≤ 6）だけ
- **検査**: `node tools/check-areas.mjs`（`npm run check` に入っている）。
  6種の効果、使えない場面、焼き込んだ手の再生で盤が一致すること、CPU 同士で盤が壊れないこと

## 画面に出すときにやること

1. `game.jsx` の START_SETUP に `areas: true` と両者の装備（`useSeats().skins`）を渡す
   （CPU戦・同じ端末の対戦から。オンラインは 3 のあと）
2. 盤の見た目（エリアごとの背景・発動ボタン・「使いました」）、凍った駒の見た目、
   変身した駒の見た目、見抜いた駒は自分にだけ表向き（`isKnownTo(state, viewer, piece)`）、
   発動の演出（`lastArea.seq` で1回だけ）。海は駒の移動アニメ
3. **オンライン**: `GAME_RULE_VERSION` を上げる。旧版の端末は `USE_AREA` を知らないので、
   同じ版どうしだけで対戦させる（`roomRuleVersion` の仕組みで可能）
4. 記録の再生（replay）は log の行で拾っている（「エリア」「宮殿」を含む行）。
   見抜いた・凍った・変身の見た目は再生でも出す
5. チュートリアルには出さない（`scripted` の対局は旗を立てない）
6. 戦略比較（`戦略比較レポート.md` の実験）を回し直して、どれかが強すぎないか見る
