# 盤面エリア

2026-09-11 更新。現在はルール版12。9×9で利用できる盤面エリアの仕様。

## 決まり

- **立つ条件**: 王に選んだ札のランクに**フォイル加工のスキン**（id の末尾が `:foil`）を
  装備していること。通常のスキンでは立たない（2026-09-09 の決め。`areaSkinOk()`）。
  ガチャ・錬成でのフォイル獲得、または同じキャラの通算100回入手による加工で手に入る。
  入手したフォイルを対応するランクに装備し、その札を王にすると使える（土なら 2 か 3）。効果の中身はスキン画面でそのフォイルを開くと見える。
  対局開始時（布陣が揃ったとき）に決まり、その局のあいだ変わらない。
  王が倒れて継承しても、エリアは最初のまま
- **見せ方**: フォイルを1枚も持たないうちは、フォイル関連（フォイル版の一覧・通常版/フォイルの切り替え・
  フォイル加工・効果盤面の説明・第13話・ルール画面の「盤面エリア」タブ・ルール設定の一言）を
  **画面に出さない**。1枚でも持てば全部見える（`foilRevealed()`）。**ガチャの提供割合のフォイルの確率だけは
  持つ前から明記**する。対局中に相手のエリアが立ったときは、盤の上の札と「?」からの説明は出る
- **発動**: 全種類が毎回の自分の手番に1回。土・森・氷は発動できる自分の手番の初めに自動発動。土で足跡がないなど、対象がない間は未使用のまま待つ。海・空・宮殿は手番の初めに「発動する／しない」を選ぶ。空・宮殿では対象の駒も指定する。海は「発動」を押さずに駒を動かせば見送りとなり、次の手番には再び選べる。
  宮殿を含め、手番を消費しない。**発動は相手にも見える**（何が起きたかは分かる）。
  見抜いた正体だけは、見抜いた側にしか見えない
- **空と宮殿で現れた札は公開**（表向き）になり、**専用のしるし**（`piece.mark`: `sky` / `palace`）が付く
- **回数**: 全種類が各手番1回。

| 王のランク | エリア | 効果 | 手番 |
| --- | --- | --- | --- |
| 2・3 | 土 | 直前に動いた相手の駒の足跡を読み、**必ず**正体を見抜く（版11から。版10までは50%）。**読んだことは相手にも分かる**。正体は自分だけが知る。相手が動いていないとき・その駒の正体をすでに知っているときは使えない | 使わない |
| 4・5 | 海 | 毎手番の初めに任意発動。**相手の駒だけ**を中央方向へ最大1マスずつ引き寄せる（版11から。版8〜10は自分の駒も流れた）。自分の駒は動かない。取りは起きない。行き先が埋まった駒はそのまま | 使わない |
| 6・7 | 森 | 相手の王以外・まだ正体を知らない駒から重複なくランダムで **2体**を見抜く。候補が1体なら1体、0体なら発動しない。対象と正体は自分だけが知る | 使わない |
| 8・9 | 氷 | 相手の駒から（版12から**王も**）、凍結中の駒も含めて **1体をランダムで**凍らせ、**相手の3手番**動けなくする。凍結中なら残り期間に相手の3手番を追加。凍った駒も通常どおり取れる。**A の入れ替えの対象にすると氷は解ける**（凍った A 自身は入れ替えを使えない）。**凍らされて何も指せなければ負け** | 使わない |
| 10 | 空 | 任意で自分の駒1体（王・10以外）を **10** に変身させる（公開・しるし付き。移動や能力も10になり、採用枚数の表は追いかける）。変身後も移動できる。以後、**自軍の10のうち、その手番に選んだ同じ1体**を2回動かせる | 使わない |
| J・Q・K | 宮殿 | 任意で自分の駒1体（王・A・K以外）を **1段階昇格**。自分は1試合に1回だけ、代わりに **2段階昇格**も選べる（9→J、10→Q、J→Kなど。Kを超える昇格は選べない）。毎手番1体まで。公開・しるし付き。初期採用合計は変えず、昇格後も移動できる。天界・魔界は同じ効果 | 使わない |

凍結中の駒を昇格・変身させても、凍結の残り期間は引き継ぐ。

### 海の引き寄せ方

版11: 相手の駒だけを、中央に近い駒から順に（同じ近さなら行・列の順）、中央方向へ縦・横・斜めに
最大1マス進める。王と凍結中の駒も対象。中央の駒と、処理時に進み先が埋まっている駒はその場に残る。自分の駒は流されず、駒の取り・凍結解除も起きない。
版8〜10で始まった対局は両者の駒を流す。版7以前で始まった対局は旧処理（中央に近い駒から順に
「中央にいちばん近い空きマス」へ詰め、18体が中央の 5×5 に収まる）で再生する。

### 氷で指せなくなったとき

手番が回ってきた側に、凍った駒のせいで指せる手が1つも無ければ、**その側の負け**
（`endReason: "frozen"`）。王が A なら入れ替えが残るので、まず起きない。

## 調整用の設定（現行版）

**効果の強さは `src/game/areas.js` の `AREA_TUNING` だけで変える。**

| 名前 | いま | 意味 |
| --- | --- | --- |
| `usesPerGame` | 1 | 旧ルール版の1局に使える回数 |
| `earthOdds` | 1 | 土で見抜ける確率（必ず成功） |
| `seaPullsOwn` | false | 海で自分の駒も流すか（現行版は相手の駒だけ） |
| `forestReveals` | 2 | 森で見抜く駒の数 |
| `iceTargets` | 1 | 氷で凍らせる駒の数 |
| `iceFreezesKing` | false | 氷で相手の王も凍らせるか（2026-09-11 の検証用。`ICE_KING=1` で検証ツールだけが有効にする） |
| `kingFreezeTurns` | null | 王の凍結手番数（null なら `freezeTurns` と同じ。調整の検証用。`KING_FREEZE=2`） |
| `kingFreezeOnce` | false | 王を凍らせるのは1局に1回だけか（調整の検証用。`KING_ONCE=1`。`areas[player].kingFrozenOnce` に記録） |
| `kingFreezeExtends` | true | 凍結中の王がもう一度選ばれたとき延長するか（調整の検証用。`KING_EXTEND=0`） |
| `freezeDeathTurns` | 0 | 凍結死。凍ったまま迎えた自分の手番がこの回数に達した駒は倒れる（0 で無効。`FREEZE_DEATH=4` で検証ツールだけが有効にする。`reducer.js` の `freezeDeaths`） |
| `freezeTurns` | 3 | 氷で相手が動けない手番の数 |
| `skyAllTens` | true | 空のあと、自軍の 10 全部を2回行動にするか（false なら変身した駒だけ） |
| `palaceCap` | "K" | 宮殿で上がれる上限（"10" にすれば 10 まで） |
| `palaceDoubleUses` | 1 | 宮殿を持つ自分が1試合に2段階昇格を選べる回数 |

効果そのものを差し替えるときは、`useArea()` の `switch (can.type)` の該当する `case` と、
`canUseArea()` の使える条件、`AREA_INFO` の文言を直す。乱数を使う効果は
`src/game/actions.js` の `enrichAction` にも足す（オンラインで両者の盤を揃えるため）。
新しい欄を手に載せるなら `firebase-rules.json` の `acts` にも足して公開する。

変えたら `node tools/check-areas.mjs`（決まりどおり動くか）と
`node tools/area-lab.mjs`（強さの目安）を回す。

### 初期ルールの参考記録：area-lab（2026-09-08、各60局、相手はエリア無し）

以下は変更前のルール・CPUによる結果で、現行版の相性表ではない。

| エリア | 持つ側の勝率 | 備考 |
| --- | ---: | --- |
| 海 | 60.8% | いちばん強い。中央に寄ると CPU の取り合いが増える |
| 森 | 48.3% | CPU は相手の伏せ札を見えているので効かない。人間相手では別 |
| 土 | 46.7% | 同上 |
| 空 | 45.8% | |
| 氷 | 45.0% | |
| 宮殿 | 43.3% | CPU は序盤にしか使わず手番を失う。使い方しだい |

CPU の腕が浅いので目安にとどめる。人間同士の感触で `AREA_TUNING` を動かす。

## チュートリアル（第13話）

`src/game/tutorial.js` の EP13。**フォイルのスキンを1枚でも持っていると開く番外の話**（`EXTRA_TUTORIALS`。
レベル設計の全12話には数えない）。9×9 で、あなたの王 10♠（空）と相手の王 8♥（氷）を1局で見せる。
台本の対局は `areas: true` と `loadouts` を台本が持ち、氷の「誰が凍るか」は `areaPicks` で固定する。
`node tools/check-tutorial.mjs` が毎回同じ盤面で最後まで通ることを見張る。

## 作り

- **状態**: `areasEnabled`（START_SETUP の `areas: true` と 9×9）、`areaLoadouts`
  （START_SETUP の `loadouts: [{rank: skinId}, {rank: skinId}]`）、`areas: [{type, used, rank, skin} | null, …]`、
  `known: [{pieceId: true}, {…}]`（見抜いた駒）、`turnNo`（氷の解ける時期を数える）、`lastArea`（演出用）
- **駒**: `frozenUntil`（氷）、`mark`（`sky` / `palace`。公開の理由を示す専用のしるし）。
  空を使った軍は `players[i].skyTwice` が立ち、その軍の 10 は全て2回動く
- **手**: `USE_AREA { pieceId?, hit?, picks? }`。空・宮殿は `pieceId` で駒を選ぶ。
  土の `hit`、森と氷の `picks`（誰を見抜く／凍らせるかの並び）は `enrichAction` が焼き込む。
  通信で届いた手にそれが無ければ捨てる（`seedsPresent`）。`NET_ACTIONS` に入っている
- **合法手**: 変身・昇格はランクそのものを書き換えるので、`getLegalMoves` はそのまま。
  `hasLegalAction`（引き分け判定）は凍った駒を数えない
- **CPU**: 選択処理は `src/game/cpu-informed.js` と `cpu.js`。海は発動前後の局面を比較し、空・宮殿も対象と発動を選ぶ
- **検査**: `node tools/check-areas.mjs`（`npm run check` に入っている）。
  6種の効果、使えない場面、焼き込んだ手の再生で盤が一致すること、CPU 同士で盤が壊れないこと

## 画面

- **CPU戦・同じ端末の対戦・対応版同士のオンライン対戦の9×9** で働く。`game.jsx` の START_SETUP に
  `areas: true` と両者の装備（`useSeats().skins`）を渡す。チュートリアル第13話では台本の装備で体験できる
- `src/ui/areas.jsx`: **AreaBar**（盤の上。自分と相手のエリア名・未使用/使用済み・海・空・宮殿の自分の番なら「発動」。
  空・宮殿は「発動」→ 駒を選ぶ → 決定）、`area-effects.jsx`（発動前の溜め→効果→余韻、6種別の装飾・効果音）
- 盤: 墓場・海賊船・ジャングル・氷海・雲海・天界・魔界の背景。両者が持つときは手番側へ切り替わる。外周の水面・葉・雲・旗・炎などを動かす
- 駒（`cards.jsx` の Piece）: 見抜いた駒は自分にだけ表向き＋「見抜」、凍った駒は青白く＋❄、
  空・宮殿で現れた札は「空」「宮」のしるし
- 手元の対局でも、席を名乗る手の乱数を `enrichAction` で焼き込むようにした（`game.jsx` の dispatch）。
  **これは上流の退行の修正も兼ねる**: 045ba0c 以降、CPU戦のサイコロ（`player` を名乗る手）が
  reducer に捨てられて振れなくなっていた

## オンラインの互換性

両者のルール版が一致し、盤面エリアに対応している場合に利用する。異なる版の組み合わせはエリアなしで開始する。
現在のルール版は12。開始済みの対局とその再生には開始時の版を適用する。
対局記録は `mark` / `frozen` / `revealed` / `known` と海の移動を保存し、再生時も表示する。

## 変更履歴

以下は各版への変更時の記録。現在の仕様は冒頭の「決まり」を参照。

### 氷の更新（ルール版4）

自分の手番が来るたび、相手の王以外の駒からランダムで1体を凍結する。凍結中の駒も同じ抽選対象。選ばれた駒が凍結中なら、残り期間へ相手の3手番を追加する（残り2手番なら5手番）。同じ手番内の重複発動は無効。旧ルール版で開始済みの対局は従来の処理を維持する。

### 全エリアの発動回数（ルール版5）

土・海・森・氷は、対象がいる自分の手番開始時に毎回自動発動。空・宮殿は、自分の手番ごとに発動するか選べる。いずれも同一手番では1回まで。発動しない手番があっても、次の手番で選べる。宮殿は従来どおり発動で手番を消費する。

### 森の対象数（ルール版6）
毎手番、相手の王以外・まだ正体を知らない駒から重複なくランダムで2体を見抜く。候補が1体なら1体だけ、0体なら発動を待つ。相手には対象の位置・正体を公開しない。

### 氷は相手の王も凍らせる（ルール版12）

- 2026-09-11。定石を整えて総当たりを取ると氷が最下位（得点率34〜41）だった。氷の要塞で6種の相手に当てた検証（`reports/fortress-tactics` 第17節）で、
  「王も凍る」だけで 48→74、「凍結死（凍ったまま4手番迎えた駒が倒れる）」だけは逆効果 41、両方だと 86 で強すぎたため、本人の判断で「王も凍る」だけを入れた。
- 凍った王は動けず、A の入れ替えでだけ解ける。凍らされて何も指せなければ負け（従来どおり）。通信の手の形式は変わらず、`picks` に王の id が入るだけ。
- 旧版で始まった対局と再生は王を除いたまま。`areas.js` の `iceFreezesKing(state)`。検証用の `AREA_TUNING.freezeDeathTurns` は既定 0 のまま残してある。

### 土は必ず見抜く・海は相手の駒だけ（ルール版11）

- 2026-09-09。同じ王同士でフォイルあり対なしを測ると、土は 49%、海は 53% で「持っても差がない」側だった
  （`reports/area-vs-none`、`reports/area-tuning`）。R のフォイルとして「持てば少し有利、SR の森・氷（62%）より下」に
  収めるため、土は当たる確率を 50% → 100%（57%）、海は自分の駒を流さず相手の駒だけ（56%）に変えた。
  空はそのまま（入手難易度が高い分、強いままでよいという決め）。検証は海が自動発動だった版で行った。
- 土の当たり外れは手（`hit`）に焼き込んであるので、旧対局の再生は変わらない。海だけルール版で切り分ける。
- 効果の数字は `AREA_TUNING.earthOdds` と `AREA_TUNING.seaPullsOwn`。検証ツールは `tools/experiment-tuning.mjs`
  経由で差し替えて測れる（`EARTH_ODDS=0.75 SEA_PULLS_OWN=1`）。
- 検証: `node tools/check-areas.mjs`（版11の海・土の当たり）、`node tools/check-field-integration.mjs`、
  `node tools/check-area-presentation.mjs`、`node tools/check-area-guide.mjs`（画面の説明が数字と揃っているか）。

### 宮殿の2段階昇格・予備札の循環（ルール版9）

- 宮殿の「発動」後に1段階／2段階を選び、対象の駒を指定する。2段階は宮殿を持つ自分が1試合に1回。キャンセル・無効な対象選択では消費しない。使い切っても通常の1段階昇格は毎手番使える。空の能力は変更しない。
- 予備札が0枚になると、両者の引き直しの捨て札・撃破された札を混ぜて新しい予備札にする。複数のJ・Qの同時撃破でも途中で補充し、各撃破に対応した予備札を引く。捨て札もなければ補充しない。配置を見送った予備札も捨て札へ戻す。
- 昇格・変身は盤上の効果。捨て札には元のランクで入れ、再配置時には凍結・王・変身などの状態を引き継がない。複数回昇格した場合も最初の数字に戻る。撃破の履歴は記録として残すが、補充済みの札を履歴から重複生成しない。
- 補充の乱数は初期のシャッフル済み山札から決定し、端末間・サーバー再生で共通の並びを使う。通信の追加項目はUSE_AREAのpromotionSteps（1または2）。旧版で始まった対局は旧仕様を維持する。
- 検証: node tools/check-palace-reserve.mjs。
