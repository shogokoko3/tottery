# 運営管理の使い方

https://tottery.shogokoko3.workers.dev/admin をPCまたはスマートフォンのブラウザで開きます。ローカルサーバーの起動は不要です。

## サインイン

Firebase Authenticationに登録済みの運営用メールアドレスとパスワードを入力します。ゲームのプレイヤー名ではログインできません。ログイン後に運営権限をサーバーで確認し、それから管理データを読み込みます。

運営アカウントは従来と同じ1アカウントです。この変更でアカウントの新規作成・権限付与・パスワード変更は行っていません。メールアドレスがわからない場合は、プロジェクト所有者がFirebase Authenticationで設定済みの運営ユーザーを確認してください。パスワードをチャットに送る必要はありません。

## プレイヤーの確認・利用停止

「プレイヤー」から名前またはIDで検索し、行を押すと詳細を確認できます。「更新」で最新の状態を読み込みます。

- 「使用停止にする」：そのアカウントの利用を停止します。確認画面を経て実行します。解除も同じ場所から行います。
- 「消す」：サーバー台帳の行だけを削除します。プレイヤーの端末内データは消えず、次に起動すると再登録される場合があります。
- 「通算成績」：9×9オンラインの従来の通算記録を確認します。月間シーズン台帳の編集には対応していません。

## お知らせ・補填

「お知らせ・補填」で宛先（全員／指定プレイヤー）・件名・本文を入力します。必要ならチケット、エーテル、経験値、称号、アイコン、スキンを添付します。配信前の確認画面で宛先と添付を確認してください。受取済みの報酬は取り消せません。

## 終了

右上の「サインアウト」を押します。ゲームのログイン情報とは保存先が別なので、ゲーム側のアカウントは切り替わりません。

## 管理範囲

この画面で扱うのはサーバー台帳・従来の通算成績・利用停止・お知らせ・待ち合わせです。各端末にある所持品・進捗の全確認や復元はできません。

ログイン画面は公開されますが、データはFirebaseの運営専用権限で保護します。`/api/admin/session` はFirebaseが検証したUIDだけを採用し、一般プレイヤーや未ログインのリクエストを拒否します。

## 成績の区分と同期

全対戦はCPU・5×5・チュートリアルを含みます。9×9オンラインの通算成績は対象対局が1戦以上ある人を掲載し、持ち点は公開ゲームと同じ加算込みの値を表示します。月間シーズン成績はサーバーで確認済みの今月の対局だけです。10戦未満でも運営画面で残り対戦数を確認できます。

プロフィール変更・対局終了・起動時に、プレイヤー情報と通算成績をまとめて保存します。通信失敗時は未送信情報を端末に残し、通信復帰・画面復帰・30秒ごとの再試行で送信します。不一致を検知すると管理画面に表示します。認証導入前の古い端末IDは、新たなランキング参加者として自動復活させません。
