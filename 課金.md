# 課金(チケットのパック・バトルパス) — 作りと、ご本人がやること

2026-09-12 に本人が決めたこと: **チケットのパック＋いまのバトルパス(買い切り)／iOS だけ／
チケットの残高と権利はサーバーの財布で持ち、鍵は Sign in with Apple**(機種変更・再インストールで消えない)。

## 作り(できているもの)

| 何 | どこ | 要点 |
| --- | --- | --- |
| 商品の目録 | `src/iap/catalog.js` | 商品 ID と、買うと何が渡るか。旗 `SHOP_ENABLED` |
| 財布(サーバー) | `src/server/wallet.js` | uid ごとの残高と権利。加減算は出来事 id で冪等、購入は Apple の取引 ID で冪等、引き継ぎは一度きり(上限500)、遊んで貯める分は 1回10枚/1日30枚 |
| Apple の署名検証 | `src/server/applejws.js` | StoreKit 2 の JWS。署名・証明書の鎖(根は Apple Root CA - G3 とバイト一致)・期限・bundleId・商品・環境・返金 |
| 口 | `src/server/worker.js` | `/api/wallet/{summary,debit,earn,migrate}`、`/api/iap/verify`。シーズン報酬のチケットはサーバーの `claim` の中で財布へ |
| アプリ側 | `src/net/wallet.js` `iap.js` `apple-signin.js` | 控え→送信→冪等。途中で落ちても二重にならない。Web では課金しない |
| 画面 | `skins.jsx`(店・先にサーバーで減らす)、`battlepass.jsx`(権利で受け取りを守る・購入・復元) | 購入の前に Apple サインインを求める(財布の鍵) |
| 検査 | `tools/check-wallet.mjs`(36件) `check-iap.mjs` | 合成した証明書鎖で署名検証も本当に回す |

**ガチャの引き方(有料時)**: 先にサーバーで減らす(`debit`) → 通ったら端末で引く(端末の枚数は減らさない。
残高はサーバーの写し)。`FREE_GACHA` が true のあいだは今まで通り無料。

## App Store Connect に作る商品(ご本人)

| 商品 ID | 種類 | 参照名(任意) | 渡るもの |
| --- | --- | --- | --- |
| `com.shogokoko.tottery.tickets.10` | 消耗型 | ガチャチケット 10枚 | チケット 10枚 |
| `com.shogokoko.tottery.tickets.30` | 消耗型 | ガチャチケット 30枚 | チケット 30枚 |
| `com.shogokoko.tottery.tickets.100` | 消耗型 | ガチャチケット 100枚 | チケット 100枚 |
| `com.shogokoko.tottery.battlepass` | 非消耗型 | バトルパス | 25マスの報酬スキンを受け取る権利 |

**商品 ID は一字も違えないこと**(アプリとサーバーが照合する)。価格は価格帯から選ぶ(目安: 10枚=10連1回ぶん)。

手順(App Store Connect → アプリ「トッタリー」→ 「App 内課金」):
1. 「＋」→ 種類を選ぶ(消耗型 / 非消耗型)→ 参照名と商品 ID を上の表のとおり → 価格 → 表示名と説明(日本語)
   → **審査用のスクリーンショット**(商品が出る画面。TestFlight の店の画面で撮ればよい)。
2. 4つとも「送信準備完了」にする。**アプリの版と一緒に審査に出す**(初回は版に紐づけて提出)。

## ご本人がやること(順番)

1. **Sign in with Apple の設定**(財布の鍵。手順書 `本人確認-Apple.md` の A-1〜A-5)。これが無いと購入ボタンで止まる。
2. **有料アプリ契約**: App Store Connect → ビジネス → 契約 → 「有料 App」に同意 → 銀行口座・税務情報を登録。
   **済むまで課金アイテムは審査に出せない**(承認に日数がかかることがある)。
3. **DSA(デジタルサービス法)**: 課金を入れて EU で配るなら「トレーダー」として住所等を公開するか、
   **配信国から EU を外す**(`App Store提出.md` 参照)。
4. **商品を作る**(上の表)。
5. **Sandbox テスター**: ユーザーとアクセス → Sandbox → テスター「＋」(メールは実在のもの。iPhone の
   設定 → App Store → Sandbox アカウントで入る)。TestFlight の店でこのアカウントで買うと課金されない。
6. **説明文と審査メモ**の「課金要素はなく、広告もありません」を書き換える(`App Store提出.md`)。
   年齢制限の「ギャンブル」は「なし」のまま(現金で直接抽選しない。チケットを買ってガチャ)。**提供割合の表示は必須のまま**(既にある)。

## iOS セッションがやること

1. Xcode → App ターゲット → Signing & Capabilities → **＋ Capability「In-App Purchase」**と**「Sign in with Apple」**。
2. `npx cap sync ios`(プラグイン `@capgo/native-purchases`、`@capacitor-community/apple-sign-in` は
   package.json に入れてあるので、これで iOS 側に入る)。
3. `npm run ios:testflight` で TestFlight へ。

## 試す順番

1. push で Worker(サーバーの口)は自動で本番に出る(済み)。古いアプリには影響しない(呼ばないだけ)。
2. 上の準備が整ったら TestFlight を配る。**`FREE_GACHA` は true のまま**でよい(ガチャは無料のまま、
   店だけ Sandbox で試せる。買ったチケットは財布に貯まる)。
3. TestFlight で: 店 → Apple サインイン → Sandbox で購入 → 残高が増える → アプリを消して入れ直し →
   Apple サインイン → 残高とバトルパスの権利が戻る(「購入を復元」)。
4. 正式リリース前: `FREE_GACHA=false`(`tools/check-submit.mjs` が見張る)。ルールの本人確認ゲートは
   クライアントを配ってから(`本人確認-Apple.md`)。

**Web の扱い**: Web には店が無いので、バトルパスは Web では今まで通り無料のまま受け取れる(行き止まりにしないため)。
iOS だけ有料。所持スキンは端末ごとなので、Web で受け取っても iOS には移らない。変えたければ `battlepass.jsx` の `owned` の条件を1行外す。

## まだ塞げていないこと(承知のうえ)

- **遊んで貯める分は端末の申告**(1回10枚・1日30枚の上限で抑えているだけ)。サーバーでミッションの達成を
  検証していない。買った分と引き継いだ分はサーバーで確かめている。
- **ガチャの抽選は端末**。サーバーは「減らす」だけで、何が当たったかは知らない。所持スキンも端末。
  次の段階で抽選と所持をサーバーへ移す(そうすればスキンも機種変更で消えない)。
- **返金**: Apple の返金通知(App Store Server Notifications)は受けていない。返金されても財布は減らない。
  必要になったら通知の口を Worker に足す。
- **引き継ぎの上限500枚**: 端末の値を信じきれないので上限を置いた。それ以上持っていた人は問い合わせで対応。
