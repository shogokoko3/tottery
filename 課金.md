# 課金(ジェム＝有償の通貨) — 作りと、ご本人がやること

2026-09-12 に本人が決めたこと: **App Store で売るのはジェムのパックだけ。チケットとバトルパスは
ゲーム内でジェムで買う。ジェムは有償のみ・有効期限なし。iOS だけ。残高と権利はサーバーの財布で持ち、
鍵は Sign in with Apple**(機種変更・再インストールで消えない)。数字(パック・値付け)は目安で、あとで相談して決める。

## 作り(できているもの)

| 何 | どこ | 要点 |
| --- | --- | --- |
| 目録と数字 | `src/iap/catalog.js` | パック(ジェム数)、チケット1枚のジェム、バトルパスのジェム。**数字はここ1か所** |
| 財布(サーバー) | `src/server/wallet.js` | uid ごとのチケット・ジェム・権利。加減算と両替は出来事 id で冪等、購入は Apple の取引 ID で冪等、遊んで貯める分は 1回10/1日30、引き継ぎ一度きり(上限500)、**未使用残高の集計 `unused()`** |
| Apple の署名検証 | `src/server/applejws.js` | StoreKit 2 の JWS。署名・証明書の鎖(根は Apple Root CA - G3)・期限・bundleId・商品・環境・返金 |
| 口 | `src/server/worker.js` | `/api/wallet/{summary,debit,earn,migrate,exchange,buy-pass}`、`/api/iap/verify`、運営だけ `/api/admin/wallet`(未使用残高) |
| アプリ側 | `src/net/wallet.js` `iap.js` `apple-signin.js`、`src/ui/gem-shop.jsx` | 控え→送信→冪等。ジェムの店は iOS で StoreKit が使えるときだけ。購入の前に Apple サインイン |
| 画面 | `skins.jsx`(有料時: ジェム残高・「チケット1枚/10枚をジェムで」・「ジェムを買う」)、`battlepass.jsx`(報酬の受け取りに権利。「バトルパスを購入(600ジェム)」) | 足りなければ店が開く |
| 検査 | `tools/check-wallet.mjs`(44) `check-iap.mjs`(17) | 合成した証明書鎖で署名検証も本当に回す。旧表からの移行も |

**流れ**: ジェムのパックを買う(StoreKit)→ 署名を Worker で検証 → 財布にジェム → ゲーム内でジェムを
チケットや権利に両替(サーバーで1つの出来事。片方だけ効くことはない)→ ガチャは先にサーバーで減らしてから引く。

## ジェムの決まり(資金決済法)

ゲーム内でしか使えない有償の通貨は、ふつう**自家型前払式支払手段**に当たる。期限なしを選んだので適用除外ではない。
- **未使用残高**(まだ使われていないジェムの円換算)が **3月末・9月末に 1,000万円を超えたら**、2か月以内に財務局へ届出、
  残高の**半分以上を供託**。超えるまでは届出も供託も不要。
- そのため **1ジェム＝1円で発行**する(未使用残高＝未使用ジェム数)。**ボーナスジェムは付けない**(付けると有償・無償を
  分けて数える必要が出る)。パックのジェム数は App Store Connect で選ぶ円の価格と同じにする。
- 残高は `/api/admin/wallet`(運営のサインインで)で出る: `unusedGems`(=円)、`issuedGems`、`usedGems`、`over`。
  **3月末・9月末に控える**。
- 利用規約と購入画面に: 使える範囲(このゲーム内だけ)、払い戻し不可、期限なし、問い合わせ先。届出後は法定の表示事項が増える。
- これは法的助言ではない。届出が近づいたら専門家に確認する。

## App Store Connect に作る商品(ご本人)

| 商品 ID | 種類 | 参照名(任意) | 渡るもの |
| --- | --- | --- | --- |
| `com.shogokoko.tottery.gems.120` | 消耗型 | 120ジェム | ジェム 120 |
| `com.shogokoko.tottery.gems.600` | 消耗型 | 600ジェム | ジェム 600 |
| `com.shogokoko.tottery.gems.1500` | 消耗型 | 1,500ジェム | ジェム 1,500 |

**商品 ID は一字も違えないこと**(アプリとサーバーが照合する)。**価格帯は「ジェム数＝円」になるものを選ぶ**
(数字を変えるときは `catalog.js` の gems と商品 ID をそろえて変える)。バトルパスは App Store の商品ではない(ジェムで買う)。

手順(App Store Connect → アプリ「トッタリー」→ App 内課金): 「＋」→ 消耗型 → 参照名と商品 ID → 価格 → 表示名と説明 →
審査用のスクリーンショット(TestFlight の店の画面)。3つとも「送信準備完了」にし、アプリの版と一緒に審査に出す。

## ご本人がやること(順番)

1. **Sign in with Apple の設定**(`本人確認-Apple.md` の A-1〜A-5)。財布の鍵。
2. **有料アプリ契約**(ビジネス → 契約 → 有料 App に同意 → 銀行口座・税務情報)。済むまで課金アイテムは審査に出せない。
3. **DSA**: EU で配るならトレーダー申告か、配信国から EU を外す。
4. **商品を作る**(上の表)。
5. **Sandbox テスター**(ユーザーとアクセス → Sandbox)。
6. 説明文と審査メモの「課金要素はなく…」を書き換える。年齢制限の「ギャンブル」は「なし」のまま。提供割合の表示は必須のまま(既にある)。
7. **数字を決める**(パック・チケットのジェム・バトルパスのジェム)→ `catalog.js` と App Store Connect をそろえる。

## iOS セッションがやること

1. Xcode → Signing & Capabilities → **In-App Purchase** と **Sign in with Apple** を足す。
2. `npx cap sync ios`(プラグインは package.json に入れてある)。
3. `npm run ios:testflight`。

## 試す順番

1. push で Worker は自動で本番へ(済み)。古いアプリには影響しない。
2. TestFlight を配る。**`FREE_GACHA` は true のまま**(ガチャは無料のまま。有料時のジェム両替の行は出ない)。
   店(ジェムを買う)はバトルパスの画面から Sandbox で試せる(25マスをそろえた状態)。
3. 正式リリース前: `FREE_GACHA=false`(`check-submit` が見張る)→ ガチャ画面にジェム残高と両替が出る。

## Web の扱い

Web には店が無い。バトルパスは Web では今まで通り無料のまま受け取れる(行き止まりにしない)。変えたければ
`battlepass.jsx` の `owned` の条件を1行外す。

## まだ塞げていないこと(承知のうえ)

- 遊んで貯めるチケットは端末の申告(上限で抑えるだけ)。ガチャの抽選と所持スキンも端末。次の段階でサーバーへ。
- 返金通知(App Store Server Notifications)は受けていない。返金されてもジェムは減らない。
- 引き継ぎの上限500枚。
- 自分の記録を消す(5.1.1(v))と未使用のジェムも消える(購入の記録は uid を伏せて残る)。
